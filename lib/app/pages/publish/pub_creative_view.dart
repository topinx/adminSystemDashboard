import 'package:flutter/material.dart';

class PubCreativeView extends StatelessWidget {
  const PubCreativeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
