import 'package:flutter/material.dart';

class PubRecommendView extends StatelessWidget {
  const PubRecommendView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
