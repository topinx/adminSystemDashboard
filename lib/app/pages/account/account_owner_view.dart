import 'package:flutter/material.dart';

class AccountOwnerView extends StatelessWidget {
  const AccountOwnerView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
