import 'package:flutter/material.dart';

class SearchManageView extends StatelessWidget {
  const SearchManageView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
