import 'package:flutter/material.dart';

class ManageEmojiView extends StatelessWidget {
  const ManageEmojiView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
