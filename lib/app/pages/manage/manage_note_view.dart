import 'package:flutter/material.dart';

class ManageNoteView extends StatelessWidget {
  const ManageNoteView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
